{
  "repository": "https://github.com/listrophy/lazy_ncaa_elm",
  "license": "Apache-2.0",
  "devDependencies": {
    "elm-css": "^0.6.0",
    "elm-live": "^2.7.4"
  },
  "scripts": {
    "build": "mkdir dist && elm-css src/Stylesheets.elm --output=dist && elm-make src/Main.elm --output=dist/index.js && cp index dist/",
    "deploy": "gh-pages-deploy"
  },
  "gh-pages-deploy": {
    "staticpath": "dist",
    "cname": "lazybracket.com",
    "prep": [
    ],
    "post": [
    ],
    "noprompt": false
  }
}
